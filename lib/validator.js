'use strict';
const validator = require('validator');

class Validator {
  constructor(obj, propertyString) {
    this.obj = obj;
    this.propertyString;
  }

  hasValue() {
    this.object.hasOwnProperty(this.propertyString);
  }
}
